<!DOCTYPE html>
<html lang="{{ metadata.language }}" class="backgroundsecondary">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://fonts.googleapis.com/css?family=Work+Sans:300">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="shortcut icon" href="/media/favicon.ico" type="image/x-icon">
  <title>{% if page.url!='/' %}{{ title }} - {% endif %}Chiro Herent</title>
</head>

<body>
  {% include 'header.njk' %}
  <section class="showcase"
    style="background-image: url('{{ showcase.image }}'); background-position: {{ showcase.placement }};">
    <h1 class="showcasetext">{{ showcase.title }}</h1>
  </section>
  <main class="{{ mainclass }}">
    {{ content | safe }}
  </main>
  {% include 'footer.njk' %}
</body>

</html>